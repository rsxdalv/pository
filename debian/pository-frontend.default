# Pository Frontend environment configuration
# Uncomment and modify as needed

# Override the backend API URL used by the dashboard
# NOTE: This variable is baked in at build time. If you change it after
# installation, re-run: cd /usr/share/pository-frontend && npm run build
# NEXT_PUBLIC_API_URL=http://localhost:3000

# Override the frontend port (default: 3001)
# PORT=3001

# Hostname to bind to (default: 0.0.0.0)
# HOSTNAME=0.0.0.0
